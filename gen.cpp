#include<cstdio>
#include<cstdlib>
#include<algorithm>
#include<random>
#include<chrono>
using namespace std;
mt19937 rnd(chrono::system_clock::now().time_since_epoch().count());
// shuffle (begin(),end(),default_random_engine());
int main(){
	freopen("in","w",stdout);
	return 0;
}
